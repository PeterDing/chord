'use strict';

import * as paths from 'path';

import { CHORD_DIR } from 'chord/preference/common/chord';


export const USER_CONFIGURATION_PATH = paths.join(CHORD_DIR, 'user.config');
export const USER_LIBRARY_PATH = paths.join(CHORD_DIR, 'library');
