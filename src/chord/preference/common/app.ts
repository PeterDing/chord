'use strict';

import * as paths from 'path';

import { CHORD_DIR } from 'chord/preference/common/chord';


export const APP_CONFIGURATION_PATH = paths.join(CHORD_DIR, 'chord.config');
export const APP_SEARCH_HISTORY_PATH = paths.join(CHORD_DIR, 'chord-search-history.json');
